import { LongholdDirective } from './longhold.directive';

describe('LongholdDirective', () => {
  it('should create an instance', () => {
    const directive = new LongholdDirective();
    expect(directive).toBeTruthy();
  });
});
